#include "processManager.h"
#include <iostream>
#include <fstream>
#include <sstream>

CProcessManager::CProcessManager() {
    // Constructor implementation
}
// function to return a struct containing running processes 
ProcessInfo CProcessManager::getRunningProcessesInfos() const {
    ProcessInfo processes[MAX_PROCESSES_COUNT];
    

    return processes;
}

// Add more functions for process management and analysis as needed
